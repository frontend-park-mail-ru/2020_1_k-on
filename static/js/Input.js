class Input {
    constructor(inputClass, regex, errorId, errorMsg) {
        this.inputClass = inputClass;
        this.regex = regex;
        this.errorId = errorId;
        this.errorMsg = errorMsg;
    }
}

export default Input;
